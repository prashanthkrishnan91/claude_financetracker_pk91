# HANDOFF — Current Repo State

Last updated: 2026-05-14 (post Stage 3.2c — remove double-click Run Intel + fix analyst rationale field loss)

## Purpose

This file is **current operational state**, not a historical log. It is meant to be loaded into context every session, so it must stay compact. Do not append PR-by-PR history. When something changes, replace or summarize the affected section instead of adding new entries.

## Current product stage

- Roadmap stage: **Stage 3.2c merged** (Continuous Intelligence Plane: snapshot prewarm + rationale field preservation). Stage 2.9 complete; Stage 2 exit still pending. Stages 2.5A–2.6D produced amount-aware Deploy v3 with new-cash sleeve sizing. Stage 2.7 turns Step 3 into the user's actual execution journal: editable actual dollar amounts per visible Deploy v3 recommendation (default = recommended), per-row status (BOUGHT / PARTIAL / SKIPPED / WATCHED / TRIMMED / SOLD / HELD), and user-added manual rows (e.g. NVDA BUY $100) clearly labelled as manual via `is_manual: true` on `ActualDecisionItem`. The Step 2 recommendation surface stays read-only. The primary Deploy UX now shows the most recent 10 decision logs below Step 3 using the existing `DecisionHistoryEntry` component (single definition; no parallel history surface). The v3 snapshot mirrors `session_key` and entered/deploy/reserve amounts into `decision_context` so existing fingerprint dedupe + history rendering work uniformly across legacy and v3 logs. Re-logging the same active plan still updates the latest matching log rather than creating duplicate spam. Plain-English clarity note added near Step 3: "These are Intel v3 planning recommendations, not broker-executed trades." No confidence engine, no new provider/market data, no Intel/Deploy sizing changes, no SQL. Frontend-only.
- Active build queue item: **Stage 2 exit validation** — re-validate end-to-end in production with a real dollar amount in Step 1: Step 2 shows 3–5 amount-aware BUY recommendations totaling exactly cash_to_deploy when no guardrail prevents it (e.g. $1,500 = $1,500, not $1,498); Step 3 lets the user edit actual amounts and add manual rows; the decision log history shows BUY spend, manual BUY, and Trim/Sell separately (never negative reserve; "Over planned by $X" / "Unallocated $X" as appropriate); Evaluate button works or gracefully reports insufficient data. Stage 2 exit remains pending until: (1) amount-aware recommendations work, (2) editable actual logging works, (3) decision log history persists after refresh, (4) journal accounting/evaluate behavior is production-validated, (5) recommendation confidence/ranking explanation is reviewed in a later evidence-quality slice.
- Current north-star reminder: Intel → Deploy → Watchtower; deterministic backend policy owns visible Buy/Hold/Trim/Sell authority. See `docs/product/NORTH_STAR.md`.

## Current architecture / runtime state

- OS v4 is the canonical operating system. No v4.2 or v5 labels.
- Visible decision authority is owned by the deterministic Intel v3 backend policy. LLMs / agents / research workers cannot own final visible action authority.
- **Intel v3 is partially certified, not fully trusted.** Run Intel v3 invokes the Evidence Refresh Orchestrator (`evidence_refresh_orchestrator_v1.py`) before deterministic decide(). The orchestrator classifies each source against the SLA contract in `evidence_freshness_contract_v1.py`, refreshes stale price/market-value evidence under deterministic budgets, and stamps a `run_mode` in snapshot diagnostics (`FAST_CERTIFIED` / `REFRESH_THEN_RUN` / `PARTIAL_CERTIFIED` / `BLOCKED_UNCERTIFIED`). `decide()` keeps sole final-action authority — the refresh path only updates inputs. **Stage 3.1 decouples the synchronous Run Intel v3 path.** The HTTP request must return a trusted snapshot quickly (~5s target) and must NOT perform any analyst/LLM/full-portfolio research inside the click. `IntelV3Service._build_analyst_refresh_callable()` wires the non-LLM `AnalystRefreshRequestSeam` into the orchestrator's `analyst_refresh` injection point; when analyst evidence is stale the seam enqueues durable `analyst_refresh_jobs` rows (idempotent upsert) and returns honest deferred-ticker accounting so the run degrades to `PARTIAL_CERTIFIED` / `BLOCKED_UNCERTIFIED` — never a fake `FAST_CERTIFIED`. **Stage 3.2 background worker** (`analyst_refresh_worker_v1.AnalystRefreshWorker`, entrypoint `analyst_refresh_worker_entrypoint.py --loop`) consumes those jobs outside the HTTP request: claims due jobs → `FullPortfolioAnalystRefreshAdapter` → `AgentOrchestrator` → writes `agent_insights` / `recommendations` (with explicit writeback bridge `analyst_evidence_writer_v1` using a fresh Supabase client) → **Stage 3.2c prewarm**: after `insights_written > 0`, calls `prewarm_intel_v3_snapshot()` → `IntelV3Service.run_prewarm_snapshot()` (zero LLM calls, skips `_run_refresh_orchestrator` entirely) → builds + persists a fresh Intel v3 snapshot → `GET /intel/v3/snapshot` returns it without any second click. Analyst verdict fields (`primary_driver`, `risk_flag`, `action_reason`, `differentiation`, etc.) from `orch._verdicts` (live `AnalystVerdict` objects) are now passed directly to the writeback writer so the stored `analyst_verdict` JSON is identical to what `_persist_sync` would write. The ticker-prefix-only rationale block is cleared when real structured rationale fields exist.
- For long architecture references, read `artifacts/Intel_v3_Architecture_Plan_Draft2_*`, `artifacts/Intel_v3_Architecture_Plan_Draft3_*`, and `artifacts/Intel_v3_Living_Cockpit_Status_Reconciliation_*` rather than copying them here.
- Runtime workflow guardrails: advisory `.claude/hooks/ai_os_advisory.py` reminds about contract / claim-safety / SQL / env paths. No blocking hooks.

## Recent meaningful PRs

Keep this section small. Only entries that affect future work; replace older lines as they age out.

- 2026-05-14 — **Stage 3.2c: Remove double-click Run Intel + fix analyst rationale field loss** — Two production root causes fixed. (1) **Double-click architecture**: worker wrote fresh `agent_insights`/`recommendations` but never built a snapshot; user had to click Run Intel a second time. Fix: after `insights_written > 0`, worker calls `_trigger_snapshot_prewarm()` → `prewarm_intel_v3_snapshot(user_id, prewarm_run_id)` → `IntelV3Service.run_prewarm_snapshot()`. Prewarm mirrors the `run_v3()` decision loop but intentionally skips `_run_refresh_orchestrator()` — no `AnalystRefreshRequestSeam`, no `analyst_refresh_jobs` rows inserted, no recursive trigger. Zero LLM calls. Persists an active snapshot so `GET /intel/v3/snapshot` returns fresh data immediately. (2) **Rationale field loss**: `_build_analyst_verdict_from_insight()` in `analyst_evidence_writer_v1` re-derived `primary_driver` from the first sentence of `investment_thesis` and set `action_reason=None` / `risk_flag=""` — because `TickerInsight` doesn't carry those fields. Fix: `default_full_portfolio_agent_orchestrator_backend` extracts `orch._verdicts` (live `AnalystVerdict` objects) after `orch.run()`, converts to dicts, passes as `verdicts=verdicts_dicts` to `write_analyst_evidence()`; `_build_analyst_verdict_from_insight(verdict_dict=...)` uses the full structured verdict directly when available. `primary_driver` / `risk_flag` / `action_reason` / `differentiation` / `why_this_matters` / `what_could_go_wrong` / `what_to_do_now` now survive end-to-end. Clears the ticker-prefix-only rationale block. Structured logs: `analyst_refresh_snapshot_prewarm_started/_completed/_failed` (with `prewarm_snapshot_id`, `prewarm_run_mode`, `prewarm_trust_status`, `prewarm_duration_ms`, `prewarm_error`) + `intel_v3_snapshot_created source=prewarm`. No SQL, no schema changes. 25 new acceptance tests; 169 existing tests pass.

- 2026-05-14 — **Stage 3.2b + 3.2: Explicit analyst evidence writeback bridge + durable analyst refresh worker** — Stage 3.2b: `AgentOrchestrator._persist_sync` silently fails in Railway worker via thread isolation; `analyst_evidence_writer_v1.write_analyst_evidence()` is the explicit fallback using a fresh `get_supabase_client()`. Idempotent by `UNIQUE(run_id, ticker)`. Stage 3.2: background worker (`AnalystRefreshWorker`) + entrypoint (`--loop`, `INTEL_V3_ANALYST_REFRESH_WORKER_INTERVAL_SECONDS` default 60s) claims `analyst_refresh_jobs` rows (SQL migration `018_analyst_refresh_jobs.sql` — apply manually), drives `FullPortfolioAnalystRefreshAdapter` outside the HTTP request. Seam also updated to idempotently enqueue durable jobs so each stale ticker gets a real consumer. Post-run readback fixed: drops fragile ticker-casing filter; scopes by `run_id`/`created_at`; maps case-insensitively. Worker never imports `decide()`. Structured logs: `analyst_evidence_writer_persisted_count`, `intel_v3.analyst_refresh_worker_run_summary`, `intel_v3.analyst_refresh_worker_loop_summary`.

- 2026-05-14 — **Stages 3.0a–3.1: Evidence Refresh Orchestrator + analyst refresh-request seam** — Stage 3.0b: `evidence_refresh_orchestrator_v1.py` classifies per-source freshness, optionally refreshes stale price under deterministic budgets, stamps `run_mode`/`trust_status`. Stage 3.0c: `FullPortfolioAnalystRefreshAdapter` replaces 6-ticker cap; runs AgentOrchestrator unscoped; post-refresh re-read of cards. Stage 3.1: `AnalystRefreshRequestSeam` decouples the synchronous HTTP path from LLM work. `IntelV3Service._build_analyst_refresh_callable()` wires the seam. Frontend amber banner for `refresh_requested` state (`analystRefreshRequestNote()` in `lib/intel-v3-banner.ts`).

- 2026-05-13 — **Stages 2.5A–2.9: Deploy v3 pipeline** — Amount-aware Deploy v3 (new-cash sleeve sizing, certified sizing source adapter, readiness diagnostic, target-allocation + policy bridges, editable execution journal, decision-log history, journal accounting, rounding residual fix, sleeve ranking by Intel conviction). Full details in `docs/product/DECISION_LOG.md`.

- 2026-05-10 — Repo cleanup: removed legacy Streamlit v1 app and added repo hygiene tooling. Full backend suite stabilized at 3,926 passed / 0 failed (before Stage 3 additions).

## Active invariants / safety packs to remember

Named packs in `docs/ai/SAFETY_PACKS_AND_ARCHETYPES.md` (Finance section) own the rules. The packs themselves are the source of truth — do not paste their contents elsewhere:

- Deterministic Decision Authority Pack
- Valuation Safety Pack
- Data Truth / Evidence Suppression Pack
- Deploy/Watchtower Boundary Pack
- Backend-only Scaffold Pack / No Visible Behavior Change Pack / Test Tier Pack (cross-cutting)

## Known risks / unresolved issues

- Deploy item pipeline (dollar math → cash guardrail → finalization → pending-reason) and plan-level rollup are wired backend-only. `tax_guardrail_status` and `wash_sale_guardrail_status` remain `not_evaluated_yet` placeholders — items reach `actionable_pending_tax` / plan reaches `ready_pending_guardrails` honestly, never `actionable`. No fully-actionable final status exists yet (rollup `actionable_count` is reserved at 0).
- Target allocation canonical source (optimizer/service) is not wired — explicit-input only for now; source wiring is deferred to a future stage.
- Watchtower trigger model is still scoped but unbuilt; no live alerts.
- Research artifact UX is intentionally deferred until decision/action loop is stable.

## Next recommended step

**Production validation of Stage 3.2c snapshot prewarm.** The full expected flow is now:

1. Click Run Intel once (evidence stale) → job enqueued, BLOCKED_UNCERTIFIED snapshot persisted
2. Worker picks up jobs within ~60s (check Railway: `intel_v3.analyst_refresh_worker_run_summary`)
3. Worker writes evidence: `analyst_evidence_writer_persisted_count=N` with `verdicts_available=N`
4. Worker prewarms snapshot: `analyst_refresh_snapshot_prewarm_completed prewarm_snapshot_id=... prewarm_run_mode=... prewarm_trust_status=...`
5. Refresh UI / `GET /intel/v3/snapshot` — no second Run Intel click needed
6. Check `intel_v3_snapshot_created source=prewarm` in logs
7. Verify no ticker-prefix-only warning in the new snapshot (check `ticker_prefix_only_reason_count=0`)

**Prerequisite (if not yet applied):** Apply `v2/database/018_analyst_refresh_jobs.sql` in Supabase SQL Editor. Ensure the Railway worker service is running with `PROCESS_TYPE=worker` and `INTEL_V3_ANALYST_REFRESH_WORKER_INTERVAL_SECONDS=60`. See `docs/ai/INTEL_V3_ANALYST_REFRESH_WORKER.md` for log keys.

**Stage 2 exit validation (re-run).** Open the Deploy page in production, enter $900 in Step 1, confirm Step 2 shows 3–5 amount-aware BUY recommendations, edit one row's actual amount, add a manual NVDA BUY $100, save, refresh, and confirm both rows persist. Confirm decision log history shows BUY spend, manual BUY, and Trim/Sell separately. Repeat with $1,500 and confirm selected BUY recommendations total exactly $1,500. Stage 2 exit remains pending on all five gates listed in Active build queue item above.

Real tax-lot / wash-sale guardrail logic is intentionally pending and stays `not_evaluated_yet` at both item and rollup levels. Parked under Build Queue → Design Pause Candidates. Must not be auto-promoted into Now by routine queue updates.

## Handoff maintenance rule

- This file is current state only. It is not an append-only log.
- Keep under ~250–500 lines. If it grows past that, **compact before adding** — summarize older sections, do not extend them.
- Every meaningful PR may update this file, but by **replacing or summarizing**, never by appending.
- Move durable historical detail to `docs/ai/MISS_LEDGER.md` (workflow/process misses) or `docs/product/DECISION_LOG.md` (product decisions). Do not preserve old noise just because it exists.
- Do not create new archive files for routine PRs. An archive is justified only when current-state value is being replaced and the original detail is still useful elsewhere.
- `CLAUDE.md`, `docs/ai/AI_REPO_OPERATING_SYSTEM.md`, and `docs/ai/PROMPT_LIBRARY.md` enforce this rule.

## Repo hygiene rule (per-PR)

Every meaningful PR must explicitly answer: **"Did this make any source files or tests obsolete?"**

- If yes: delete or rewrite them in the same PR, **or** add a tracked follow-up entry with a one-line reason.
- Run `python3 scripts/repo_hygiene/audit_repo_hygiene.py` before opening the PR. Treat the report as a merge-gate aid, not mandatory CI.
- Rules, allowlist conventions, and test-retirement criteria live in `docs/ai/REPO_HYGIENE.md`.
- `v2/progress_log.md` follows the convention at the top of that file: ~150–250 lines, current state only, no PR-by-PR append.


## Purpose

This file is **current operational state**, not a historical log. It is meant to be loaded into context every session, so it must stay compact. Do not append PR-by-PR history. When something changes, replace or summarize the affected section instead of adding new entries.

## Current product stage

- Roadmap stage: **Stage 3.0c merged** (Intel v3 Full-Portfolio Analyst Evidence Refresh, replacing the Stage 3.0b.6 6-ticker default). Stage 2.9 complete; Stage 2 exit still pending. Stages 2.5A–2.6D produced amount-aware Deploy v3 with new-cash sleeve sizing. Stage 2.7 turns Step 3 into the user's actual execution journal: editable actual dollar amounts per visible Deploy v3 recommendation (default = recommended), per-row status (BOUGHT / PARTIAL / SKIPPED / WATCHED / TRIMMED / SOLD / HELD), and user-added manual rows (e.g. NVDA BUY $100) clearly labelled as manual via `is_manual: true` on `ActualDecisionItem`. The Step 2 recommendation surface stays read-only. The primary Deploy UX now shows the most recent 10 decision logs below Step 3 using the existing `DecisionHistoryEntry` component (single definition; no parallel history surface). The v3 snapshot mirrors `session_key` and entered/deploy/reserve amounts into `decision_context` so existing fingerprint dedupe + history rendering work uniformly across legacy and v3 logs. Re-logging the same active plan still updates the latest matching log rather than creating duplicate spam. Plain-English clarity note added near Step 3: "These are Intel v3 planning recommendations, not broker-executed trades." No confidence engine, no new provider/market data, no Intel/Deploy sizing changes, no SQL. Frontend-only.
- Active build queue item: **Stage 2 exit validation** — re-validate end-to-end in production with a real dollar amount in Step 1: Step 2 shows 3–5 amount-aware BUY recommendations totaling exactly cash_to_deploy when no guardrail prevents it (e.g. $1,500 = $1,500, not $1,498); Step 3 lets the user edit actual amounts and add manual rows; the decision log history shows BUY spend, manual BUY, and Trim/Sell separately (never negative reserve; "Over planned by $X" / "Unallocated $X" as appropriate); Evaluate button works or gracefully reports insufficient data. Stage 2 exit remains pending until: (1) amount-aware recommendations work, (2) editable actual logging works, (3) decision log history persists after refresh, (4) journal accounting/evaluate behavior is production-validated, (5) recommendation confidence/ranking explanation is reviewed in a later evidence-quality slice.
- Current north-star reminder: Intel → Deploy → Watchtower; deterministic backend policy owns visible Buy/Hold/Trim/Sell authority. See `docs/product/NORTH_STAR.md`.

## Current architecture / runtime state

- OS v4 is the canonical operating system. No v4.2 or v5 labels.
- Visible decision authority is owned by the deterministic Intel v3 backend policy. LLMs / agents / research workers cannot own final visible action authority.
- **Intel v3 is partially certified, not fully trusted.** Run Intel v3 invokes the Evidence Refresh Orchestrator (`evidence_refresh_orchestrator_v1.py`) before deterministic decide(). The orchestrator classifies each source against the SLA contract in `evidence_freshness_contract_v1.py`, refreshes stale price/market-value evidence under deterministic budgets, and stamps a `run_mode` in snapshot diagnostics (`FAST_CERTIFIED` / `REFRESH_THEN_RUN` / `PARTIAL_CERTIFIED` / `BLOCKED_UNCERTIFIED`). `decide()` keeps sole final-action authority — the refresh path only updates inputs. **Stage 3.1 decouples the synchronous Run Intel v3 path.** The HTTP request must return a trusted snapshot quickly (~5s target) and must NOT perform any analyst/LLM/full-portfolio research inside the click. `IntelV3Service._build_analyst_refresh_callable()` now wires the non-LLM `AnalystRefreshRequestSeam` (`analyst_refresh_request_seam_v1.py`) into the orchestrator's `analyst_refresh` injection point instead of an LLM adapter. When analyst evidence (`recommendations` / `agent_insights`) is stale the seam logs `intel_v3.analyst_refresh_requested` and returns honest deferred-ticker accounting (`analyst_refresh_status="refresh_requested"`, zero LLM calls) so the run degrades to `PARTIAL_CERTIFIED` (stale) or `BLOCKED_UNCERTIFIED` (hard-stale) — never a fake `FAST_CERTIFIED`. The seam does NOT enqueue a durable job; a future continuous Intelligence Plane PR owns consumption. The LLM adapters (`analyst_refresh_adapter_v1.py`, `full_portfolio_analyst_refresh_adapter_v1.py`) are retained in the repo for that background plane but are no longer wired into the synchronous request. Tier-0 price/market-value refresh still runs under deterministic budgets and routes through `PriceService.fetch_prices` → per-ticker coalescing locks (the Intel v3 callable also dedupes its ticker list). Diagnostics still expose `analyst_refresh_*` fields (`analyst_refresh_status`, `analyst_refresh_deferred_tickers`, etc.) plus `banner_age_summary`. Frontend: `analystRefreshRequestNote()` in `lib/intel-v3-banner.ts` renders an honest amber note under the snapshot banner when `analyst_refresh_status="refresh_requested"` — no fake queue/background-job language.
- For long architecture references, read `artifacts/Intel_v3_Architecture_Plan_Draft2_*`, `artifacts/Intel_v3_Architecture_Plan_Draft3_*`, and `artifacts/Intel_v3_Living_Cockpit_Status_Reconciliation_*` rather than copying them here.
- Runtime workflow guardrails: advisory `.claude/hooks/ai_os_advisory.py` reminds about contract / claim-safety / SQL / env paths. No blocking hooks.

## Recent meaningful PRs

Keep this section small. Only entries that affect future work; replace older lines as they age out.

- 2026-05-14 — **Stage 3.2b: Explicit analyst evidence writeback bridge (production-blocker fix)** — Production evidence (Railway logs post-PR #318) showed `agent_run_status=completed`, `agent_run_insight_count=34`, but `insights_by_run_id=0`, `recs_by_run_id=0`, `missing_reason_breakdown={'agent_run_completed_no_persisted_rows': 34}`. Root cause: `AgentOrchestrator._persist_sync` runs via `asyncio.to_thread(self._persist_sync, state)` using `self.db` (instance-level Supabase client); in the Railway worker process that client silently fails inside the thread — exception caught at orchestrator.py:567–575 so `run()` returns `completed` with 34 insights in memory but zero DB rows. The working `_read_post_run_evidence` uses a fresh `get_supabase_client()` confirming the issue is the instance client in threads. Fix: new `analyst_evidence_writer_v1.py` (`write_analyst_evidence` async function + `_write_sync` inner) uses a fresh `get_supabase_client()` — NOT the orchestrator's instance client — and is called in `full_portfolio_analyst_refresh_adapter_v1.default_full_portfolio_agent_orchestrator_backend` after `orch.run()` returns `status="completed"` with non-empty insights, before the `_read_post_run_evidence` readback. Idempotent: checks existing `agent_insights` (`UNIQUE(run_id, ticker)`) and `recommendations` rows first; only inserts missing ones. `analyst_verdict` built from real `TickerInsight` fields (`used_fallback=False` — LLM ran; `analysis_source="explicit_writeback"`); no fabricated verdicts. Step 5: expires stale active recs from prior runs only when new rows were written. `created_at=now_iso` set on all written rows so `ReadOnlyEvidenceAdapter` freshness timestamps work. Writer logs `analyst_evidence_writer_persisted_count`, `insights_written`, `recs_written`, `already_present_*`, `write_error`. Does NOT replace `_persist_sync` — that path still runs first; this is the guaranteed safety net when it fails. Never imports deterministic decision policy. No fabricated success if both paths fail. 74 backend tests in `test_intel_v3_stage_3_2_analyst_refresh_worker.py` (all green): conviction-level mapping, `analyst_verdict` field contract, unit writeback with idempotency / partial-already-present / empty-insights no-op / write-error → write_error field set (not raised) / expire-old-recs only when new rows written; integration: write-then-readback from zero-rows state, worker-marks-succeeded via explicit writeback backend, orch-raised backend writes nothing + honest failure, `ReadOnlyEvidenceAdapter` reads explicitly-written rows as fresh, honest failure when both paths fail. No SQL, no new providers, no Deploy/Watchtower change, no decide() authority change. Supabase SQL: None.

- 2026-05-14 — **Stage 3.2: Continuous Intelligence Plane v1 — durable analyst evidence refresh worker** — Stage 3.1 left the refresh-request seam log-only ("no durable job"); production stayed `analyst_refresh_status=refresh_requested` with no consumer. Stage 3.2 builds the first real background plane. New SQL migration `v2/database/018_analyst_refresh_jobs.sql` (`analyst_refresh_jobs` table — operational/service-role-only, RLS deny-all like migration 007; unique index `(user_id,ticker,refresh_window)` is the idempotency key; due-job index on `(status,next_retry_at,requested_at)`) — **Supabase SQL: Yes, manual apply.** New `analyst_refresh_job_store_v1.py` (idempotent `enqueue_refresh_jobs` — exactly one row per `(user,ticker,window)`; called only on an **explicit user-triggered Run Intel v3**, so it makes jobs claimable *now* rather than honouring the worker-backoff timer: `pending` touched, in-flight `claimed` (within `STALE_CLAIM_TIMEOUT_SECONDS`=600s) touched, **stale `claimed` reopened** (abandoned-claim recovery), **`failed`-with-attempts-remaining made due now** — status→pending + next_retry_at→now, attempts preserved — `failed`-exhausted + `succeeded` reopened with attempts reset; emits `created`/`touched`/`made_due`/`reopened`/`reopened_failed`/`failed_not_due_before`/`statuses_before`/`statuses_after`/`next_retry_min,max` diagnostics. `claim_due_jobs` — guarded single-row claim, skips future-retry/exhausted jobs; `mark_job_succeeded`/`mark_job_failed` — exponential backoff 15m→24h governs *automatic* worker retries only, no fabricated freshness). New `analyst_refresh_worker_v1.py` (`AnalystRefreshWorker.run_once()` — claims due jobs under job/runtime budget caps, groups by user, prioritises owned BUY/TRIM first via `prioritize_stale_tickers`, drives the existing `FullPortfolioAnalystRefreshAdapter` outside the HTTP request, marks per-ticker outcome; partial success persists per ticker; runtime-budget-exhausted / adapter-crash → all jobs `failed` with retry, never succeeded). New runnable entrypoint `analyst_refresh_worker_entrypoint.py` (`python -m ... --loop`; Railway = separate worker service, shares `v2/backend/railway.toml` with web service via `PROCESS_TYPE=worker` env var; `Procfile` gains a `worker:` line). **The worker is polling, not event-driven** — Run Intel v3 enqueues/touches jobs but does NOT wake the worker; it picks them up on its next poll. Loop interval is `INTEL_V3_ANALYST_REFRESH_WORKER_INTERVAL_SECONDS` (**default 60s**; invalid/missing/non-positive → 60s fallback; `--interval-seconds` CLI flag overrides). Each poll logs `intel_v3.analyst_refresh_worker_loop_summary` (`interval_seconds`/`next_poll_at`/claimed/selected/succeeded/failed). For production validation set the env var to 60. **Production-blocker readback fix (p03):** `full_portfolio_analyst_refresh_adapter_v1._read_post_run_evidence` previously filtered every post-run query with `.in_("ticker", <UPPER-cased worker tickers>)`, but `AgentOrchestrator` persists `agent_insights`/`recommendations` with the ticker as stored in `positions` (no casing normalisation) — so any casing mismatch excluded every row server-side and the worker reported `no_post_run_evidence` for all tickers. Fixed by dropping the fragile ticker filter: the readback now scopes purely by the durable key (`run_id`/`agent_run_id` + `user_id` + `created_at`) and maps per-ticker case-insensitively, plus emits a `full_portfolio_analyst_refresh.post_run_readback` diagnostics line. Verification honesty unchanged (`insight_run_match`/`used_fallback` still gate success). **Durable-persistence attribution (p02 of #318):** even after the casing fix, production showed the worker running the agent path but `agent_insights`/`recommendations` getting NO fresh rows — the orchestrator's `_persist` isn't writing in the worker context. The readback was blind to *why*: every no-rows ticker collapsed to opaque `no_post_run_evidence`. `default_full_portfolio_agent_orchestrator_backend` now captures the `AgentPipelineResult.status` (or the exception `orch.run()` raised) and threads it into `_read_post_run_evidence`, which attributes a SPECIFIC reason — `agent_run_raised:<Exc>` / `agent_run_failed` / `agent_run_no_data` / `agent_run_completed_no_persisted_rows` — and logs `agent_run_status` in `post_run_readback`. The worker still honestly fails those jobs (no fabricated success); the specific reason pinpoints the orchestrator stage for the durable-persistence follow-up. The Intel v3 certified read path (`ReadOnlyEvidenceAdapter`) is test-verified to read worker-written `recommendations`/`agent_insights` rows as fresh once they exist. The seam (`analyst_refresh_request_seam_v1.py`) now takes an optional `client` and idempotently enqueues durable jobs when wired; `IntelV3Service._build_analyst_refresh_callable()` passes `self.client`. Worker never imports the deterministic decision policy (test-enforced + verified absent from `sys.modules`). Run Intel v3 unchanged as a fast zero-LLM certification path — the only added in-request work is one idempotent queue upsert. Observability: structured logs for enqueue/claim/select/succeed/fail/run-summary (keys in `docs/ai/INTEL_V3_ANALYST_REFRESH_WORKER.md`). Tests: new `test_intel_v3_stage_3_2_analyst_refresh_worker.py` (38 backend: enqueue idempotency for all five states — pending/claimed/failed-retryable touched, succeeded + exhausted-failed reopened-when-still-stale — claim/retry/exhaustion, worker consumes-outside-request, partial-success persistence, no-fabricated-freshness on crash/timeout/build-failure, decide-not-imported, run_v3 zero-LLM + enqueues exactly one durable job per stale ticker + repeated clicks no duplicates + end-to-end run_v3→worker drain, post-run readback ticker-casing + run-id contract). Also fixed a wall-clock-fragile Stage 3.1 test (`_run_v3_with_evidence` now anchors patched evidence ages to the real clock the orchestrator uses). Focused Stage 3.1 + Stage 3.2 + orchestrator/freshness/refresh/router suites green. Railway worker config fix: shared `v2/backend/railway.toml` with shell conditional on `PROCESS_TYPE` env var — web service unset/empty, worker service `PROCESS_TYPE=worker`. No new providers, no Deploy/Watchtower/broker/tax, no decide() authority change, no UI work.
- 2026-05-14 — **Stage 3.1: Decoupled synchronous Run Intel v3 + analyst refresh-request seam** — Root-cause architecture fix: Stages 3.0b.6 / 3.0c wired the LLM `AgentOrchestrator` directly into the synchronous Run Intel v3 HTTP request, making the click attempt comprehensive live research. Stage 3.1 makes the synchronous path: load persisted evidence/cards → classify freshness → optional Tier-0 price refresh under strict budget → deterministic `decide()` → certify/persist → return fast. New `analyst_refresh_request_seam_v1.py` (`AnalystRefreshRequestSeam`) plugs into the orchestrator's existing `analyst_refresh` injection point with the same callable contract but does ZERO LLM/provider work — on stale analyst evidence it logs `intel_v3.analyst_refresh_requested` and returns `status="refresh_requested"` with stale tickers as `deferred_tickers`, so the run honestly degrades to `PARTIAL_CERTIFIED` / `BLOCKED_UNCERTIFIED` (never fake `FAST_CERTIFIED`). **Stage 3.2 turns the seam into a real durable mechanism.** The seam now idempotently enqueues a durable `analyst_refresh_jobs` row (SQL migration `v2/database/018_analyst_refresh_jobs.sql`, unique on `user_id,ticker,refresh_window`) per stale ticker — a fast queue upsert, not LLM work, so the synchronous contract holds. `IntelV3Service._build_analyst_refresh_callable()` passes `self.client` into the seam so the synchronous path enqueues; the seam is still log-only when no client is wired. A separate worker (`analyst_refresh_worker_v1.AnalystRefreshWorker` + runnable entrypoint `analyst_refresh_worker_entrypoint`) claims due jobs, prioritises owned BUY/TRIM first, drives the existing `FullPortfolioAnalystRefreshAdapter` (→ `AgentOrchestrator` → persisted `recommendations`/`agent_insights`) OUTSIDE the HTTP request, and marks per-ticker outcome — failed tickers stay `failed` with exponential-backoff retry, never fabricated success. The worker never imports the deterministic decision policy. Job store: `analyst_refresh_job_store_v1` (`enqueue_refresh_jobs`/`claim_due_jobs`/`mark_job_succeeded`/`mark_job_failed`). Runtime/observability reference: `docs/ai/INTEL_V3_ANALYST_REFRESH_WORKER.md`. `_build_price_refresh_callable()` dedupes its ticker list ahead of `PriceService.fetch_prices` (which already coalesces per-ticker). Frontend: `lib/intel-v3-banner.ts::analystRefreshRequestNote()` + amber banner line for the refresh-requested state. Tests: new `test_intel_v3_stage_3_1_decouple_run_v3.py` (17 backend: seam unit, orchestrator-with-seam zero-LLM, run_v3 zero analyst/LLM calls, honest refresh-requested state, deterministic `decide()` authority preserved across fresh vs stale, no LLM-adapter imports in service, price dedupe/coalescing); `test_intel_v3_full_portfolio_analyst_refresh_v1.py::TestServiceAdapterSelection` rewritten for the seam wiring; new frontend `intel-v3-banner.test.ts` (11 banner-copy contract tests). All 219 Intel v3 / orchestrator / freshness / refresh backend tests green; frontend banner + IntelV3 contract suites green. No SQL, no new providers, no Deploy/Watchtower change, no decide() authority change, no background scheduler/worker added.
- 2026-05-14 — **Stage 3.0c: Intel v3 Full-Portfolio Analyst Evidence Refresh** — Root-cause fix for the Stage 3.0b.6 production blocker. Trace: Run Intel v3 → `/api/v1/intel/v3/run` → `IntelV3Service.run_v3()` → Step 1 `ReadOnlyEvidenceAdapter.load_cards()` (pre-refresh) → Step 1b `EvidenceRefreshOrchestrator` → `AnalystRefreshAdapter` (Stage 3.0b.6) → `AgentOrchestrator` SCOPED to `analyst_refresh_tickers={top-6}` → DB writes for 6 of 34 tickers → 28 tickers still HARD_STALE → `classify_run_mode` → `BLOCKED_UNCERTIFIED`. Two failures: (1) the 6-ticker default cap meant 28 of 34 positions could never refresh in a single click, so the run mode never left `BLOCKED_UNCERTIFIED`; (2) even when LLM calls succeeded, `run_v3()` continued to build decisions from the pre-refresh `cards` loaded in Step 1 instead of re-reading durable evidence. Stage 3.0c: new `full_portfolio_analyst_refresh_adapter_v1.py` wraps `AgentOrchestrator` UNSCOPED (no `analyst_refresh_tickers` filter), passes every stale active position into the same full-portfolio LLM pass that recommendation_engine's `recommendations.aggregate.done` log already proves works (34 tickers, 35 attempted / 35 successful LLM calls, ~5s, 0 failed). Per-ticker success read back from real DB rows by matching `agent_insights.run_id` / `recommendations.agent_run_id` against the new agent run; failed / fallback / unpersisted tickers keep their stale ages. `_build_analyst_refresh_callable` now defaults to `FullPortfolioAnalystRefreshAdapter` and exposes `INTEL_V3_ANALYST_REFRESH_MODE` for explicit emergency `budgeted_subset` (the legacy 6-cap adapter). After the orchestrator returns at least one successful analyst ticker, `run_v3()` re-calls `ReadOnlyEvidenceAdapter.load_cards()` so deterministic `decide()` consumes the refreshed rows instead of the pre-refresh snapshot. Old failing log pattern: `analyst_refresh_status=succeeded attempted_llm_calls=6 successful_llm_calls=6 ... run_mode=BLOCKED_UNCERTIFIED`. New expected log pattern: `analyst_refresh_status=succeeded attempted_llm_calls=34 successful_llm_calls=34 ... run_mode=REFRESH_THEN_RUN trust_status=trusted` plus `intel_v3.post_refresh_reread ... refreshed_tickers=34`. 21 new backend tests; all 2520 Intel v3 / orchestrator / freshness / refresh tests green. No SQL, no new providers, no Deploy / Watchtower / broker / tax change, no decide() authority change, no UI redesign. **Production validation gates:** (1) `intel_v3_freshness_summary` shows `attempted_llm_calls` near the full active position count (not 6) and `analyst_refresh_status="succeeded"`; (2) successful tickers' `recommendations.created_at` / `agent_insights.created_at` move to near-current in actual DB rows; (3) banner reports near-zero ages: "Analyst evidence: X hours old. Recommendation evidence: Y hours old."; (4) `run_mode=REFRESH_THEN_RUN` with `trust_status=trusted` when no critical source is still HARD_STALE; (5) any genuine failure (timeout / fallback / persistence miss) stays partial or blocked honestly with `analyst_refresh_failed_tickers` listed.
- 2026-05-14 — **Stage 3.0b.6: Intel v3 Analyst LLM Refresh Adapter v1** — New `analyst_refresh_adapter_v1.py` wraps the existing `AgentOrchestrator` as a narrow, budgeted bridge that the Evidence Refresh Orchestrator can inject as `analyst_refresh`. Adapter prioritizes stale tickers deterministically (BUY/TRIM first, then portfolio weight, then evidence age, then ticker A→Z) and caps to `DEFAULT_MAX_ANALYST_TICKERS_PER_RUN=6` / `DEFAULT_MAX_ANALYST_LLM_CALLS_PER_RUN=6` / `DEFAULT_MAX_ANALYST_REFRESH_SECONDS=90`. Per-ticker success/failure is sourced from real post-run `agent_insights` / `recommendations` row state (`created_at >= started_at` AND non-fallback verdict); failed tickers stay stale with no fabricated timestamps. `AgentOrchestrator` gained an optional `analyst_refresh_tickers: set[str]` kwarg so the per-ticker analyst phase + persistence + recommendations-expire only touch the selected subset; non-scope tickers' active rows are preserved untouched. `IntelV3Service._run_refresh_orchestrator` builds priority hints from `recommendations.action` + `portfolio_snapshots` weights + per-ticker `agent_insights.created_at`, passes them through `OrchestratorInputs.per_ticker_evidence`, and wires the adapter. The orchestrator's `_post_refresh_inputs` now honors per-ticker analyst success — partial refresh only collapses ages for refreshed tickers, never for failed/deferred tickers (no fabricated freshness). Diagnostics expose `analyst_refresh_per_ticker` / `analyst_refresh_selected_tickers` / `analyst_refresh_deferred_tickers` / `analyst_refresh_successful_tickers` / `analyst_refresh_failed_tickers`. Banner truth fix: `snapshot_freshness_diagnostics.build_evidence_freshness()` now also emits `banner_age_summary` — plain English like "Analyst evidence: 12.0 days old. Recommendation evidence: 8.0 days old." — and the `FreshnessLine` in `IntelV3Cockpit.tsx` consumes it (with a frontend-side fallback for older snapshots), replacing the previous "Oldest evidence: 8.0 days" that silently dropped agent insight age. Env: `INTEL_V3_ANALYST_REFRESH_ENABLED=0` disables the adapter; default-on. 27 new backend tests in `test_intel_v3_analyst_refresh_adapter_v1.py` covering priority order, budget cap subset selection (20 stale + budget=5), per-ticker success honesty, used_fallback-not-success rule, partial-success staying PARTIAL/BLOCKED, full-success reaching REFRESH_THEN_RUN + TRUST_TRUSTED, honest LLM accounting, skipped_budget zero-call path, backend timeout, backend exception, Tier 0 price refresh intact regression, no Deploy/Watchtower imports in adapter, adapter never sets visible action, adapter never writes `intel_v3_snapshots`, banner_age_summary plain-English contract. All 177 Intel v3 / orchestrator / freshness tests green (35 + 27 + 116 + 21). Pre-existing failure in `test_stage_2_5b_snapshot_market_values.py::test_gate_k_enriched_snapshot_exact_dollar_ready_true` unchanged (verified on main pre-PR). 0 SQL, 0 new providers, 0 Deploy/Watchtower change, no broker/tax, no decide() authority change. **Production-validation plan:** with stale inventory (recs ~193h / insights ~287h, 34 active positions), Run Intel v3 should now (1) attempt analyst refresh on the top-6 priority stale tickers under budget, (2) successful tickers move their `recommendations.created_at` / `agent_insights.created_at` forward in real DB rows, (3) `intel_v3_freshness_summary` log emits non-zero `attempted_llm_calls` / `successful_llm_calls`, (4) when ≤6 of 34 actually refresh, `run_mode` stays `BLOCKED_UNCERTIFIED` (most tickers still HARD_STALE); to clear the block, the adapter would need a higher per-run budget OR multiple runs over time. **Next exact follow-up:** observe one or more production runs, tune budgets if needed, then move toward Stage 3.0b.9 production validation. Watchtower stays parked.
- 2026-05-13 — **Stage 3.0b v1+addendum: Evidence Refresh Orchestrator + Investment Intelligence Platform foundations** — On top of the orchestrator slice below, this PR adds: (1) `docs/ai/INVESTMENT_INTELLIGENCE_PLATFORM_NORTH_STAR.md` — durable 12-target architecture doc (market data plane / evidence graph / provider registry / refresh tiers / event-driven invalidation / decision replay / confidence calibration / portfolio-intel vs opportunity-scout / runtime / failure / performance / Stage 3.0b v1 scope + explicit next blockers); (2) `evidence_artifact_contract_v1.py` — pure canonical `EvidenceArtifact` dataclass with all north-star §2 fields, deterministic content-hash `evidence_id`, `is_inside_sla()`, `with_downgraded_trust()`, and mappers for `recommendations`, `agent_insights`+`agent_runs`, portfolio positions, and price results. `allowed_policy_axis` is the spine that keeps portfolio_state out of the `action` axis and persisted analyst evidence out of `sizing`/`portfolio_fit`. Mappers calibrate trust deterministically from age vs SLA — no fabricated freshness; production-stale rows (191.8h recs, 286.1h insights) classify UNCERTIFIED automatically; (3) `provider_registry_v1.py` — pure central catalog seeded with 11 known providers (yfinance, alpaca, finnhub, polygon, coingecko, sec_edgar, sec_companyfacts, agent_orchestrator, claude_analyst, research_workers, portfolio_service) across 14 source classes, with per-provider env-gating, freshness SLA, batch support, fallback priority, failure mode, cost tier, test strategy. `health_summary(env)` reports fallback provider per source class + disabled-provider list with the env var that would enable each. Orchestrator diagnostics now embed `provider_registry_health` so any future PR can ask "is analyst_thesis blocked because ANTHROPIC_API_KEY isn't set?" without scattered env reads. 32 new backend tests (artifact contract: 17 covering deterministic ids, SLA gating, policy-axis isolation, all 4 mappers, production-stale calibration; provider registry: 15 covering seed, env-gating, fallback ordering, health summary, orchestrator integration). 80 backend tests total in the Stage 3.0b cluster (all green). 0 SQL, 0 new providers (registry catalogs what already exists), 0 Deploy/Watchtower change. **Next exact blocker:** Stage 3.0b.6 — narrow `analyst_refresh` adapter injecting `agent_orchestrator` into the orchestrator's existing injection point under the LLM budget.
- 2026-05-13 — **Stage 3.0b v1: Intel v3 Evidence Refresh Orchestrator (vertical slice)** — New pure module `evidence_freshness_contract_v1.py` owns per-source SLA windows (recommendations/agent_insights critical 24h/168h, prices 15m/4h, etc.), source-state classification (FRESH/STALE/HARD_STALE/MISSING/UNKNOWN), and run-mode classifier (FAST_CERTIFIED/REFRESH_THEN_RUN/PARTIAL_CERTIFIED/BLOCKED_UNCERTIFIED), plus single-source-of-truth banner copy. New `evidence_refresh_orchestrator_v1.py` runs as Step 1b inside `IntelV3Service.run_v3()` before deterministic decide(): builds `OrchestratorInputs` from existing evidence_stats + latest portfolio_snapshots + active positions, classifies pre-refresh, optionally calls `price_engine.PriceService.fetch_prices()` for stale tickers under deterministic budgets (`MAX_PROVIDER_CALLS_PER_RUN=50`, `MAX_LLM_CALLS_PER_RUN=10`, `MAX_TOTAL_REFRESH_SECONDS=30`), re-classifies post-refresh, and returns a `RefreshResult` whose `to_diagnostics_dict()` carries `run_mode` / `trust_status` / `banner_copy` / `source_freshness` per source class / `attempted_*_calls` / `successful_*_calls` / `failed_*_calls` / `refreshed_source_count` / `failed_refresh_count` / `analyst_refresh_supported` / `analyst_refresh_status` / `budget_exhausted` / `orchestrator_notes`. `snapshot_freshness_diagnostics.build_diagnostics()` accepts the orchestrator dict (back-compat preserved when `refresh_diagnostics=None`) and merges it into snapshot diagnostics; `evidence_mode` is renamed to the post-refresh certified mode when refresh ran. `intel_v3_freshness_summary` log line now emits `run_mode`, `trust_status`, real provider/LLM call counts, `refreshed_source_count`, `failed_refresh_count`, `analyst_refresh_supported`, `analyst_refresh_status`, `budget_exhausted`. Frontend: `IntelV3SnapshotDiagnostics` extended with optional Stage 3.0b fields (`run_mode`, `trust_status`, `banner_copy`, `source_freshness`, refresh counts, `analyst_refresh_*`); `IntelV3RunMode` / `IntelV3TrustStatus` / `IntelV3SourceFreshness` types exported. `SnapshotBanner` renders plain-English run-mode label ("Fresh certified", "Refreshed stale evidence before running", "Partial: some evidence stale or unavailable", "Blocked: current evidence unavailable") and tone (green only when FAST_CERTIFIED or successful REFRESH_THEN_RUN; amber for PARTIAL_CERTIFIED; red for BLOCKED_UNCERTIFIED). **v1 scope:** price/market-value refresh wired; analyst LLM refresh path NOT wired in v1 — orchestrator exposes the injection point and honestly reports `analyst_refresh_supported=False` / `analyst_refresh_status="not_supported_v1"`. Production-stale analyst inventory (recommendations 191.8h, agent_insights 286.1h) classifies BLOCKED_UNCERTIFIED — never silently FAST_CERTIFIED. 27 new backend tests (all pass) covering: production-like stale ages don't go FAST_CERTIFIED, all-fresh goes FAST_CERTIFIED, stale price → REFRESH_THEN_RUN, failed refresh → PARTIAL_CERTIFIED, BLOCKED_UNCERTIFIED preserved, provider budget caps, LLM budget caps, repeated identical inputs are stable, no fabricated freshness after a failed refresh, diagnostics shape contract, orchestrator does NOT import `decide`/Deploy/Watchtower. 0 SQL, no new providers, no Deploy/Watchtower change, no broker, no policy threshold change, no decide() authority change. Next exact slice (3.0b.6): narrow analyst-refresh adapter on top of `AgentOrchestrator`.
- 2026-05-13 — **Stage 3.0a: Intel v3 snapshot freshness + decision-diff diagnostics** — Added `snapshot_freshness_diagnostics.py` (pure module; no IO/LLM/provider) with `build_evidence_freshness()`, `build_decision_diff()`, and `build_diagnostics()`. `ReadOnlyEvidenceAdapter.load_cards()` now selects `recommendations.created_at` and maps `agent_runs.finished_at` per ticker's insight run — timestamps returned in `evidence_stats` dict (`recommendation_timestamps`, `agent_insight_run_timestamps`). `build_snapshot()` accepts additive `diagnostics` param; embedded in snapshot JSONB payload. `intel_v3_service.run_v3()` fetches the previous active snapshot before persisting the new one, computes diagnostics (freshness + diff), and emits a compact `intel_v3_freshness_summary` log line including snapshot_id, action_counts, evidence_mode, max evidence age, changed_decision_count, previous_snapshot_id, attempted_llm_calls=0, live_provider_calls=0. Frontend: `IntelV3SnapshotDiagnostics` type added to `api.ts`; `IntelV3Cockpit.tsx` `SnapshotBanner` shows a one-line plain-English freshness note ("Intel v3 refreshed deterministic policy using stored evidence. Oldest evidence: Xh. Decisions changed: 0."). Timestamps available: `recommendations.created_at` (per active ticker rec) and `agent_runs.finished_at` (for each ticker's insight run). Not available: per-insight `created_at` (not selected; `agent_runs.finished_at` is the right proxy). No policy change, no LLM, no provider, no SQL migration, no Deploy/Watchtower scope. 21 new backend pure tests (all pass). Intel v3 runtime freshness marked partially certified — pending production validation.
- 2026-05-13 — **Stage 2.9: Deploy v3 new-cash sleeve ranking + selection_reason** — Replaced accidental input-order top-N in `apply_new_cash_sleeve_sizing` (`deploy_new_cash_sleeve_v1.py`) with deterministic ranking by Intel conviction (HIGH > MEDIUM > LOW), then evidence band (STRONG > OK/PARTIAL > THIN), with ticker A→Z as the stable tie-breaker. The previous comment claimed "deterministic input-order ranking" but selection was actually a function of Intel snapshot card order — re-shuffling the same 10 BUYs could change which 5 were sized. Weak/missing/stale/blocked BUYs are still suppressed upstream by the translation layer and never reach the ranker (re-asserted in test). Added optional `intel_conviction`, `intel_evidence_band`, `selection_reason` passthrough fields to `DeployPlanItem` (defaults so existing call sites keep working); `_translate_item` now copies conviction/band from the Intel adapter; selected BUYs get a plain-English `selection_reason` ("high-conviction BUY with strong evidence from Intel v3.") derived from existing labels — no invented confidence, no raw metric jargon. Mapper `derivePlainReason` surfaces `selection_reason` for BUY rows; UI Step 2 "why" line in `deposits/page.tsx` now reads it automatically. 4 new backend ranking tests in `test_deploy_new_cash_sleeve_v1.py` (27 sleeve tests / 554 deploy tests, all green) + 2 frontend mapper tests. No Intel v3 authority changes, no LLM, no SQL, no providers, no Watchtower.
- 2026-05-13 — **Stage 2.8: Deploy v3 journal accounting + Evaluate restore + rounding residual** — Decision-log history accounting is now action-aware. New pure helpers `classifyActualAction` and `computeJournalTotals` in `deploy-v3-decision-log.ts` separate BUY spend (BOUGHT/PARTIAL/REPLACED), manual BUY subset (from `is_manual` rows), Trim/Sell (TRIMMED/SOLD), and skipped (SKIPPED/WATCHED/HELD). The shared `DecisionHistoryEntry` in `deposits/page.tsx` no longer renders a single `Invested / Reserved` aggregate (which could go negative when TRIM/SELL/manual rows were included). It now shows "BUY spend $X", optionally "incl. manual $Y", "Trim/Sell $Z" when present, and either "Over planned by $X" (when BUY > planned cash) or "Unallocated $X" (when BUY < planned cash) — never negative. Manual rows carry a "Manual" badge in the expanded ticker list; TRIM/SELL rows render in amber. Evaluate is preserved on every history row: Deploy v3 snapshots now mirror BUY rows into `recommendation_snapshot.normalized_tickers` so the existing backend evaluation hook/service handles Deploy v3 logs uniformly; TRIM/SELL rows are excluded from performance math; manual rows do not crash evaluation (not in normalized_tickers); insufficient 7d/30d/90d data continues to render gracefully via existing per-window status labels. Backend `apply_new_cash_sleeve_sizing` in `deploy_new_cash_sleeve_v1.py` now distributes leftover whole dollars from floor rounding to selected BUY rows deterministically (top-ranked first, never to suppressed/below-min slots, never beyond cash_to_deploy) — $1,500 selected BUY rows now usually total exactly $1,500 instead of $1,498. No Intel v3 / Deploy decision authority changes. No SQL, no provider, no LLM, no broker. 28 new tests (5 backend rounding-residual + 8 frontend journal accounting + 2 normalized_tickers + classifyActualAction + page-wiring) → 85 frontend deploy-v3-decision-log tests / 23 backend sleeve tests, all green.
- 2026-05-13 — **Stage 2.7: Editable Deploy v3 execution log + decision-log history restore** — `DeployV3DecisionLogSection` in `v2/frontend/src/app/dashboard/deposits/page.tsx` now renders editable actual-amount inputs (default = recommended), per-row status dropdown (BOUGHT / PARTIAL / SKIPPED / WATCHED / TRIMMED / SOLD / HELD), and an "add manual action" form (ticker / BUY|TRIM|SELL / amount / optional note). Manual rows are visually badged ("Manual"), removable, and tagged `is_manual: true` in the payload via the new `buildDeployV3ManualRow` helper. Recommended rows preserve `recommended_amount` + `recommended_action` alongside the edited `actual_amount`. The new payload field `ActualDecisionItem.is_manual` is the only api.ts contract change (additive optional). Step 3 also adds the plain-English note "These are Intel v3 planning recommendations, not broker-executed trades." Below Step 3, the v3 path now reuses the existing `DecisionHistoryEntry` to render the latest 10 deduped logs from `useDecisionMemoryLogs(10, true)`; the v3 snapshot mirrors `session_key` and entered/deploy/reserve amounts into `decision_context` so `dedupeDecisionLogsForDisplay` / `getDecisionLogSessionKey` and history rendering work uniformly. Active-plan fingerprint update preserved — `buildDeployV3SessionKey(v3Plan?.run_id, step2.items)` lookup triggers `updateLog.mutateAsync` for the matching log instead of a duplicate create. Legacy `DecisionLogMemoryPanel` retained intentionally (only used by the legacy fallback `DeploymentPlan` path; never co-renders with the v3 path). 8 new pure-helper / page-wiring tests in `deploy-v3-decision-log.test.ts` (346 total frontend tests pass). 0 backend changes, no SQL, no providers, no LLM.
- 2026-05-13 — **Stage 2.6D: Deploy v3 new-cash sleeve sizing v1** — replaces BUY-side current-gap math in new-cash mode with sleeve sizing in `deploy_new_cash_sleeve_v1.py`: eligible universe = Intel v3 BUY ACTIONABLE_CANDIDATE only; deterministic input-order selection capped at 5; cash_to_deploy distributed across selected items using saved target_weight as a relative guardrail (equal-weight fallback); floor rounding caps total ≤ cash_to_deploy; minimum trade threshold drops below-threshold selections. HOLD/TRIM/SELL never receive new-cash BUY dollars. TRIM/SELL still use current-gap. Adds `new_cash_residual_usd` / `new_cash_residual_reason` to `DeployPlan` and surfaces `source.residual_cash` + `source.residual_reason` in the router response. 18 new backend tests (production-like 10-BUY $900 case asserts 3–5 BUYs and ≥$450 deployed); api.ts adds two optional source fields; no SQL, no providers, no LLM, no broker.
- 2026-05-13 — **Stage 2.6C production patch: amount propagation fix** — `deposits/page.tsx` now gates `useDeployV3Plan` on `deployV3Enabled = Number.isFinite(amount) && amount > 0`; passes `undefined` (not `0`) when disabled so deposits page never subscribes to the base `["deploy_v3","plan"]` key that `DeployV3Panel` populates. Fixes stale no-cash React Query cache hit when field is cleared. 5 new URL/guard contract tests (68 total mapper tests; all pass). Frontend-only; no backend/SQL/provider changes.
- 2026-05-13 — **Stage 2.6C: Amount-aware Deploy v3 new-cash planning v1** — `cash_to_deploy` optional query param added to `GET /api/v1/deploy/v3/plan`; BUY delta now `target_weight * (portfolio + cash) - current_value` in new-cash mode; total BUY capped at `cash_to_deploy`; cash guardrail uses planning capital (not persisted $0 balance); TRIM/SELL unchanged; `amount_aware`/`cash_to_deploy`/`sizing_mode` in source metadata; frontend passes Step 1 amount into hook; Step 2 and Step 3 copy branch on `amount_aware`; 25 new backend + 20 new frontend tests; no SQL.
- 2026-05-12 — **Stage 2.6B: Deploy v3 decision logging v1** — `DeployV3DecisionLogSection` replaces Step 3 placeholder in Deploy v3 path; `deploy-v3-decision-log.ts` pure helpers (`buildDeployV3DecisionSnapshot`, `buildDeployV3InitialActualDecisions`, `buildDeployV3SessionKey`); snapshot records `source: "deploy_v3"`, Step 1 amount as context only, visible Step 2 items exactly; no_moves/setup_incomplete → no fake log; 35 contract tests in deploy-v3-decision-log.test.ts; 301 total; 0 backend changes; no SQL.
- 2026-05-12 — **Stage 2.6A: Deploy v3 powers Step 1/2/3 flow (patched ×2)** — Step 1/2/3 primary UX; Deploy v3 Step 2 data source; `deploy-v3-step2-mapper.ts` pure mapper; Step 2/3 coherence enforced; 46 mapper contract tests; 266 total; 0 backend changes; no SQL.
- 2026-05-12 — **Stage 2.5F: Deploy v3 target allocation setup flow v1** — `DeployV3TargetSetupPanel` on Deploy page; editable target % rows for all position tickers; live total with 98–102% gate enforced; "use current weights as draft" (market values required, not auto-save); save calls `PUT /api/v1/portfolio/targets`; invalidates `["deploy_v3","readiness"]` + `["deploy_v3","plan"]` + `["portfolio","targets"]`; `useSetDeployTargets()` hook; `PolicyGuidance` section for missing Railway env vars (lists names and allowed values; never exposes values). 29 new contract tests; 200 total; 0 failed. No backend changes, no SQL.
- 2026-05-12 — **Stage 2.5E: Deploy v3 readiness UI surface v1** — `DeployV3ReadinessPanel` on Deploy page; calls `GET /api/v1/deploy/v3/readiness`; renders exact_dollar_ready gate summary, snapshot status (missing/stale/fresh), market value coverage (uncertified tickers), target allocation gaps (missing/conflicting tickers, total %), policy status (missing_minimum_trade/missing_rounding_policy/invalid/certified — no values). `useDeployV3Readiness()` hook with query key `["deploy_v3", "readiness"]`; `DEPLOY_V3_READINESS_ENDPOINT` constant; `DeployV3ReadinessDiagnostic` TypeScript type; `policyStatusLabel()` helper. Loading/error/missing-data/flag-off states do not crash. 43 new frontend contract tests; all pass. No backend changes, no SQL.
- 2026-05-12 — **Stage 2.5D: Deploy v3 production readiness diagnostic v1** — `GET /api/v1/deploy/v3/readiness` (authenticated, read-only); reports all readiness gates, snapshot metadata (id, age, stale/fresh), per-ticker market-value coverage, target allocation gaps + total %, policy section (`minimum_trade_configured`, `rounding_policy_configured`, `policy_valid`, `policy_status`; no values exposed), suppression reasons, plain-English `next_required_action`. `deploy_readiness_diagnostic_v1.py` wraps the sizing adapter; zero LLM/provider/broker calls. 49 new backend tests (42 diagnostic + 7 router readiness; router tests cover registration, GET method, flag-off→404, delegation, no-legacy imports, auth); 651 deploy tests total; 0 failed. Backend-only, no SQL, no UI.
- 2026-05-12 — **Stage 2.5C: Deploy v3 target-allocation + policy readiness hardening v1** — portfolio-level target allocation total bounds (98%–102%) enforced in `target_allocation_ready` and `get_suppression_reasons` (Deploy v3 has no explicit cash/residual target contract; near-full specification required for exact-dollar readiness); duplicate DB rows → CONFLICTING trust; invalid policy config fails safe (UNSUPPORTED). 3 new suppression reasons in `DeploySizingSuppressionReason`; `TARGET_ALLOCATION_TOTAL_MIN/MAX` constants added. 46 new tests + updated 38 existing tests to use valid target totals (≥98%); 485 deploy tests total; 0 failed. Backend-only, no SQL, no UI.
- 2026-05-12 — **Stage 2.5B: Deploy v3 snapshot market-value source v1** — `PortfolioService.create_snapshot()` now enriches `positions_data` with `market_price_usd`, `market_value_usd`, `market_value_source`, `market_value_certified_at` when price is valid and fresh. Omits fields when price missing/stale/invalid (fail-safe). Cost basis never stored as market value. Price fetch failure falls back gracefully. Reuses price service cache — no second provider fetch. 33 new backend tests; 72 total for adapter + enrichment (0 failed). No SQL, no new providers, no LLM, no deploy decision changes.
- 2026-05-12 — **Stage 2.5A: Deploy v3 certified sizing source adapter v1** — `deploy_sizing_source_adapter_v1.py` adapter reads `portfolio_snapshots` + `target_allocations` + Settings; certifies sizing inputs via deterministic staleness (24h) and explicit market-value check (cost basis never promoted). Returns `None` when no snapshot exists or DB read fails; returns a non-ready bundle when sources exist but gates fail; only `exact_dollar_ready=True` when all three gates certified. Wired into `GET /api/v1/deploy/v3/plan`; source metadata expanded with `exact_dollar_ready`, `sizing_values_ready`, `target_allocation_ready`, `policy_ready`, `suppression_reasons`. `deploy_minimum_trade_usd` / `deploy_rounding_policy` added to Settings. 51 new backend tests (39 adapter + 12 router); 4469 total backend; 0 failed. Small frontend compatibility patch: `DeployV3Panel` sizing disclaimer now based on `exact_dollar_ready` instead of `sizing_bundle_provided` (stays visible when bundle exists but exact-dollar readiness not yet met); `DeployV3PlanResponse.source` type extended; 24 new frontend contract tests (49 total). No SQL, no providers, no LLM, no broker/execution.
- 2026-05-12 — **Stage 2.4B: Deploy v3 read-only UI surface** — `DeployV3Panel.tsx` component; `api.deployV3.getPlan()` calling `/api/v1/deploy/v3/plan`; `useDeployV3Plan()` hook with query key `["deploy_v3", "plan"]`. Panel renders plan readiness label, counts (pending/blocked/informational/suppressed/not-ready), Intel v3 authority note, and honest sizing-not-connected disclaimer. Handles loading / 404 no-snapshot / flag-off / error / empty states without crashing. Does not call legacy `/allocation/plan` or `/api/deposit-plan` for Deploy v3 data. Legacy deposit workflow untouched. 25 new frontend contract tests; 0 new backend changes. No SQL. No providers. No LLM.
- 2026-05-12 — **Stage 2.4A: Deploy v3 read-only API endpoint** — added `app/routers/deploy_v3.py`; registered in `main.py`. `GET /api/v1/deploy/v3/plan`: authenticated, read-only, mirrors Intel v3 feature flag, returns 404/`no_snapshot` when no snapshot, calls `build_deploy_inputs_from_snapshot` → `build_deploy_plan(sizing_bundle=None)` → returns `plan_status`, `items`, `guardrail_summary`, `rollup`, `source` metadata. No sizing bundle → dollar fields null, `exact_dollar_math_evaluated=false`, BUY/TRIM `final_actionability_status=not_ready` (honest). Legacy `/allocation/plan` unchanged. No UI, no SQL, no providers, no LLM, no broker, no Watchtower. 25 new tests; 4418 total; 0 failed.
- 2026-05-12 — **Stage 2.3E: Deploy plan-level readiness rollup v1** — `deploy_plan_rollup_v1.py` (`build_plan_rollup`, `DeployPlanRollup`); deterministic `plan_readiness_status` ladder; fail-safe unknown bucket. Backend-only; no API/route at that stage.
- 2026-05-11 — **Stages 2.3A–2.3D: Deploy exact-dollar pipeline + per-item finalization** — `deploy_dollar_math_v1.py` (Stage 2.3A; `exact_dollar_ready` gate; BUY/TRIM/SELL deltas; rounding & min-trade suppression; share-quantity only with certified price). `deploy_cash_guardrail_v1.py` (Stage 2.3B; cash certified vs blocked vs not-applicable). `deploy_finalization_v1.py` (Stage 2.3C; per-item `final_actionability_status` ∈ informational_hold | suppressed | blocked_cash | actionable_pending_tax | not_ready). Stage 2.3D added `pending_guardrails_reason` (deterministic across all paths; cleared on non-pending finalization). Item-level intel_action / actionability_status / dollar amount / cash status are never mutated by later stages.
- 2026-05-11 — **Stages 2.0–2.2: Deploy foundation, sizing input contract, policy + target-allocation bridges** — `deploy_contracts.py` (DeployPlan, DeployPlanItem, DeployGuardrailSummary, enums), `deploy_intel_adapter.py` (read-only Intel v3 snapshot reader), `deploy_translation_v1.py` (BUY/TRIM/SELL scaffold; HOLD never actionable; THIN/stale/blocked/weak suppressed; PriceBand never an authority). `deploy_sizing_contracts.py` + `deploy_sizing_builder.py` (Stage 2.1; trust/suppression model; three readiness gates). `deploy_policy_bridge.py` + `deploy_target_allocation_bridge.py` (Stage 2.2; explicit-source-only certification; rejects placeholder labels and fabricated weights).
- 2026-05-10 — Phase 14F closure: added hidden backend-only visible context scaffold (`priceband_visible_context_v1.py`) behind a disabled-by-default config flag. No visible behavior changes, no route, no snapshot writes, no DecisionInputV3 mutation, no Buy/Hold/Trim/Sell changes. **Closed as hidden scaffold only.**
- 2026-05-10 — Final test-suite cleanup: backend full-suite stabilized at **3,926 passed / 0 failed**; 5 stale tests retired with one-line justifications (no production code changed). `audit_repo_hygiene.py` gained an async-test antipattern check. Full backend suite is now a Tier-3 release/infra gate — see `docs/ai/TEST_ROUTING.md`.
- 2026-05-10 — Repo cleanup: removed legacy Streamlit v1 app and added repo hygiene tooling. Deleted `v1/`, root `App.py`, root `requirements.txt`, `.streamlit/`, `.devcontainer/`, the v2 `migration_service.py`, and the `/api/v1/positions/seed-v1` endpoint (zero callers). Compressed `v2/progress_log.md` to a current-state log under the new convention; deleted `v2/progress_log_archive.md`. Added `docs/ai/REPO_HYGIENE.md` and the read-only `scripts/repo_hygiene/audit_repo_hygiene.py` audit. v2 is now the only active product surface.
- 2026-05-10 — workflow architecture hygiene completed (claude-flow stack, helpers, legacy `.claude/commands/`, ~75 stale/duplicate workflow assets, root-surface clutter all removed; canonical anchors are `.claude/skills/`, `docs/ai/TEST_ROUTING.md`, `docs/ai/PROMPT_LIBRARY.md`, `.github/pull_request_template.md`, `docs/ai/SAFETY_PACKS_AND_ARCHETYPES.md`). Active docs/configs no longer reference deleted assets.
- Earlier Intel v3 / Deploy-prep / evidence-check copy work has been folded into product source-of-truth docs and is no longer tracked PR-by-PR here. See `docs/product/DECISION_LOG.md` and `docs/ai/MISS_LEDGER.md` for durable records.

## Active invariants / safety packs to remember

Named packs in `docs/ai/SAFETY_PACKS_AND_ARCHETYPES.md` (Finance section) own the rules. The packs themselves are the source of truth — do not paste their contents elsewhere:

- Deterministic Decision Authority Pack
- Valuation Safety Pack
- Data Truth / Evidence Suppression Pack
- Deploy/Watchtower Boundary Pack
- Backend-only Scaffold Pack / No Visible Behavior Change Pack / Test Tier Pack (cross-cutting)

## Known risks / unresolved issues

- Deploy item pipeline (dollar math → cash guardrail → finalization → pending-reason) and plan-level rollup are wired backend-only. `tax_guardrail_status` and `wash_sale_guardrail_status` remain `not_evaluated_yet` placeholders — items reach `actionable_pending_tax` / plan reaches `ready_pending_guardrails` honestly, never `actionable`. No fully-actionable final status exists yet (rollup `actionable_count` is reserved at 0).
- Target allocation canonical source (optimizer/service) is not wired — explicit-input only for now; source wiring is deferred to a future stage.
- Watchtower trigger model is still scoped but unbuilt; no live alerts.
- Research artifact UX is intentionally deferred until decision/action loop is stable.

## Next recommended step

**Production validation of Stage 3.2b explicit writeback bridge.** The worker should now reliably write `agent_insights`/`recommendations` rows even when `_persist_sync` silently fails in the Railway worker context. Validate: (1) Run the worker and watch for `analyst_evidence_writer_persisted_count=N` log lines (`N>0`). (2) Confirm `recommendations.created_at` and `agent_insights.created_at` move to near-current in real DB rows for the worked tickers. (3) Confirm `_read_post_run_evidence` finds those rows (`insight_run_match=True`), the worker marks jobs `succeeded`, and the Intel v3 seam no longer returns `BLOCKED_UNCERTIFIED` due to stale analyst evidence. (4) If `analyst_evidence_writer_persisted_count=0` but `insights_written=0 already_present_insights=N`, the legacy `_persist_sync` is now working in production — that is also a success state. If `write_error` is non-null, investigate the specific error class.

**Prerequisite (if not yet applied):** Apply `v2/database/018_analyst_refresh_jobs.sql` in the Supabase SQL Editor and confirm the worker entrypoint runs: `python -m app.services.intelligence.v3.analyst_refresh_worker_entrypoint --loop`. See `docs/ai/INTEL_V3_ANALYST_REFRESH_WORKER.md` for the full validation gate and log keys.

**Stage 3.1 still wants its own production confirmation** (fast ~5s request, honest `refresh_requested` + PARTIAL/BLOCKED run mode, amber UI note) — Stage 3.2 does not change the synchronous request behaviour, only adds the durable enqueue + background consumer.

**Stage 2 exit validation (re-run).** Open the Deploy page in production, enter $900 in Step 1, confirm Step 2 shows 3–5 amount-aware BUY recommendations, edit one row's actual amount (NFLX $245 → $250), add a manual NVDA BUY $100, save, refresh, and confirm both rows persist with `is_manual` correctly set. Confirm the decision log history shows BUY spend, manual BUY, and Trim/Sell separately and never displays a negative reserve. Repeat with $1,500 and confirm selected BUY recommendations total exactly $1,500 (rounding residual distributed). Click Evaluate on a fresh log and confirm it either reports performance or "Insufficient data yet." without crashing. Stage 2 exit remains pending on: (1) amount-aware recommendations, (2) editable actual logging, (3) decision log history persists after refresh, (4) journal accounting/evaluate behavior, (5) recommendation confidence/ranking explanation in a later evidence-quality slice. Do not move to Stage 3 / Watchtower until all five are validated in production.

Real tax-lot / wash-sale guardrail logic is intentionally pending and stays `not_evaluated_yet` at both item and rollup levels until a separately scoped design lands (it requires explicit decisions on tax-lot / trade-history source, cost-basis model, and wash-sale window scope). It is parked under Build Queue → Design Pause Candidates and Later, and must not be auto-promoted into Now by routine queue updates.

## Handoff maintenance rule

- This file is current state only. It is not an append-only log.
- Keep under ~250–500 lines. If it grows past that, **compact before adding** — summarize older sections, do not extend them.
- Every meaningful PR may update this file, but by **replacing or summarizing**, never by appending.
- Move durable historical detail to `docs/ai/MISS_LEDGER.md` (workflow/process misses) or `docs/product/DECISION_LOG.md` (product decisions). Do not preserve old noise just because it exists.
- Do not create new archive files for routine PRs. An archive is justified only when current-state value is being replaced and the original detail is still useful elsewhere.
- `CLAUDE.md`, `docs/ai/AI_REPO_OPERATING_SYSTEM.md`, and `docs/ai/PROMPT_LIBRARY.md` enforce this rule.

## Repo hygiene rule (per-PR)

Every meaningful PR must explicitly answer: **"Did this make any source files or tests obsolete?"**

- If yes: delete or rewrite them in the same PR, **or** add a tracked follow-up entry with a one-line reason.
- Run `python3 scripts/repo_hygiene/audit_repo_hygiene.py` before opening the PR. Treat the report as a merge-gate aid, not mandatory CI.
- Rules, allowlist conventions, and test-retirement criteria live in `docs/ai/REPO_HYGIENE.md`.
- `v2/progress_log.md` follows the convention at the top of that file: ~150–250 lines, current state only, no PR-by-PR append.
